#include <bits/stdc++.h>
#include <iostream>
using namespace std;
class Solution
{
public:
    unordered_map<char, int> m;
    bool helper(int k)
    {
    }
    int longestSubstring(string s, int k)
    {
        int n = s.size(), sol = INT_MIN;
        for (int i = 0; i <= 5; i++)
        {
            /* code */
        }
    }
};
int main()
{

    return 0;
}