#include <bits/stdc++.h>
#include <iostream>
using namespace std;
struct ListNode
{
    int val;
    ListNode *next;
};
class Solution
{
public:
    ListNode *sortList(ListNode *head)
    {
        

    }
    // vector<int> mee;
    // ListNode *currtnr = head;
    // while (currtnr != NULL)
    // {
    //     mee.push_back(currtnr->val);
    //     currtnr = currtnr->next;
    // }
    // sort(mee.begin(), mee.end());
    // currtnr=head;
    // for (int i = 0; i < mee.size(); i++)
    // {

    //     currtnr->val = mee[i];
    //     currtnr = currtnr->next;
    // }
    // return head;
};

int main()
{

    return 0;
}