#include <bits/stdc++.h>
#include <iostream>
using namespace std;
class Solution
{
public:
    int helper(vector<vector<int>> board, int n, int m, int i, int j)
    {
        int noofone = 0;
        if (i + 1 < m)
        {
        }
        if (i - 1 < 0)
        {
        }
        if (i + 1 < m)
        {
        }
        if (i + 1 < m)
        {
        }
    }
    void gameOfLife(vector<vector<int>> &board)
    {
        vector<vector<int>> sol;
        int n = board.size(), m = board[0].size();
        for (int i = 0; i < n; i++)
        {
            for (int j = 0; j < m; j++)
            {
                if (board[i][j] == 0)
                {
                }
            }
        }
    }
};
int main()
{

    return 0;
}